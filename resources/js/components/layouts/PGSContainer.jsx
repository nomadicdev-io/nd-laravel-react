import { ContainerStyled } from "../../styles/common"

const PGSContainer = ({children}) => {
  return (
    <ContainerStyled>
        {children}
    </ContainerStyled>
  )
}

export default PGSContainer